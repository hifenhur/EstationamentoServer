<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:card_types, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:card_types, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :card_type, url(:card_types, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'card_types/form', :locals => { :f => f } %>
 <% end %>
</div>
