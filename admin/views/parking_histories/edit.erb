<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:parking_histories, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:parking_histories, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:parking_histories, :edit, :id => @parking_history.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :parking_history, url(:parking_histories, :update, :id => @parking_history.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'parking_histories/form', :locals => { :f => f } %>
  <% end %>
</div>
