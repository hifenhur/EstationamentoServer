<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:parameters, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:parameters, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:parameters, :edit, :id => @parameter.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :parameter, url(:parameters, :update, :id => @parameter.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'parameters/form', :locals => { :f => f } %>
  <% end %>
</div>
