<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:payments, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:payments, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:payments, :edit, :id => @payment.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :payment, url(:payments, :update, :id => @payment.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'payments/form', :locals => { :f => f } %>
  <% end %>
</div>
